#include "main.h"

/**
 * add - a function that that adds two integers and returns the result.
 * Prototype: int add(int i, int j);
 * @i: an integer.
 * @j: an integer.
 * Return: The sum of the two numbers.
 */

int add(int i, int j)
{
	return (i + j);
}
