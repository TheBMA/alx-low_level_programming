#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * Prototype: int mul(int a, int b);
 * @a: int to multiply
 * @b: int to multiply
 * Return: product.
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
