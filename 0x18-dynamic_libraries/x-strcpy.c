#include "main.h"

/**
 * _strcpy - an empty function for the library.
 * Prototype: char *_strcpy(char *dest, char *src);
 * @dest: a variable.
 * @src: a variable.
 * Return: a string of characters.
 */

char *_strcpy(char *dest, char *src)
{
	return (src);
}
