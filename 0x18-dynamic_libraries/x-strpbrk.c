#include "main.h"

/**
 * _strpbrk - an empty function for the library.
 * Prototype: char *_strpbrk(char *s, char *accept);
 * @s: a string
 * @accept: a string
 * Return: a string
 */

char *_strpbrk(char *s, char *accept)
{
	return (s);
}
