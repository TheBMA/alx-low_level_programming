#include "main.h"

/**
 * _strspn - an empty function for the library.
 * Prototype: unsigned int _strspn(char *s, char *accept);
 * @s: a string
 * @accept: a string
 * Return: 0
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
