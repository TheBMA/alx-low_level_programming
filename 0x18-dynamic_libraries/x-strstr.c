#include "main.h"

/**
 * _strstr - an empty function for the library.
 * Prototype: char *_strstr(char *haystack, char *needle);
 * @haystack: a string
 * @needle: a string
 * Return: a string
 */

char *_strstr(char *haystack, char *needle)
{
	return (haystack);
}
